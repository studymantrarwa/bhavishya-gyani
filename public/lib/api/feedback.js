const {authUser,req:sbreq,json}=require('./_lib');module.exports=async(req,res)=>{try{const u=await authUser(req);if(!u)return json(res,401,{error:'Login required'});if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});const b=req.body||{};const rating=Math.max(1,Math.min(5,Number(b.rating||0)));const text=String(b.text||'').slice(0,2000);if(!rating)return json(res,400,{error:'Rating चुनें'});await sbreq('/rest/v1/app_feedback',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({user_id:u.id,rating,feedback:text})});return json(res,201,{ok:true,message:'Feedback saved'})}catch(e){return json(res,500,{error:e.message})}}