V59 — PERFORMANCE / FAST NAVIGATION / CHAT OPEN FIX

Apply this patch on top of V58/current project. Do NOT run supabase/setup.sql.

Changes:
- Chat opens its mobile chat screen immediately with a loading state instead of waiting for the conversation API response.
- Chat conversation loading, conversation-list loading and realtime setup no longer block each other unnecessarily.
- Existing realtime channel is reused for the same conversation instead of being torn down/recreated on every refresh.
- mark-read and realtime setup run in the background after the chat UI is rendered.
- Added service-worker navigation caching for the static HTML shell while /api/* remains network-only.
- Added same-origin link prefetch on pointer/touch interaction.
- This reduces the landing-page flash when using Back and makes common feature navigation feel faster after the first visit.
- No database schema changes.
