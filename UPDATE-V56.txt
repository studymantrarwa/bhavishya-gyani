Bhavishya Gyani V56 — Kundli Share + Scroll Stability Fix

Apply this patch on top of V55/current project.

Fixes:
- Kundli share list uses cached Kundli data and retries transient network fetch failures.
- Avoids browser "Failed to fetch" on transient /api/my-kundlis network failures by retrying.
- Adds Retry button inside the Kundli picker instead of breaking the chat page.
- Prevents background page scrolling while the Kundli picker is open.
- Selecting a Kundli no longer causes the message list to jump to the top/bottom unexpectedly.
- New incoming/optimistic messages only auto-scroll when the user is already near the bottom.
- Selected Kundli is reused from cache when sending, avoiding a second /api/my-kundlis request.
- No database/schema/setup.sql changes.
