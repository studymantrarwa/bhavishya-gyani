V57 PATCH — visible End Chat button + Kundli selection highlight

Applied on top of V56.

Changes:
- Added a clear red “End Chat” button in the chat header/profile area.
- The button is available to both User and Astrologer while a chat is active/requested.
- It calls the existing close/end-chat API and preserves User chat history.
- The button hides after the conversation is already closed/missed/rejected.
- Made selected Kundli highlight much clearer: gold border, glow, and “✓ यही Kundli share होगी”.
- No database/schema/setup.sql changes.
