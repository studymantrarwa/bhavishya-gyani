const {authUser,req:sbreq,json,profile}=require('./_lib');
const esc=v=>encodeURIComponent(String(v));
module.exports=async(req,res)=>{try{
 const u=await authUser(req);if(!u)return json(res,401,{error:'Login required'}); const p=await profile(u.id); if(p?.blocked)return json(res,403,{error:'Account blocked'});
 if(req.method==='GET'){const id=String(req.query.conversation_id||'');if(!id)return json(res,400,{error:'conversation_id required'});const c=(await sbreq(`/rest/v1/conversations?id=eq.${esc(id)}&select=*`))[0];if(!c||![c.user_id,c.astrologer_id].includes(u.id))return json(res,403,{error:'Forbidden'});const rows=await sbreq(`/rest/v1/messages?conversation_id=eq.${esc(id)}&select=*&order=created_at.asc&limit=1000`);return json(res,200,{messages:rows,conversation:c})}
 if(req.method==='POST'){const b=req.body||{},id=String(b.conversationId||'');if(!id||!String(b.body||'').trim())return json(res,400,{error:'conversationId and message required'});const c=(await sbreq(`/rest/v1/conversations?id=eq.${esc(id)}&select=*`))[0];if(!c||![c.user_id,c.astrologer_id].includes(u.id))return json(res,403,{error:'Forbidden'});if(c.status!=='accepted')return json(res,409,{error:'Chat is not active'});const rows=await sbreq('/rest/v1/messages',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({conversation_id:id,sender_id:u.id,body:String(b.body).trim().slice(0,4000),kundali_id:b.kundaliId||null})});await sbreq(`/rest/v1/conversations?id=eq.${esc(id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({last_message_at:new Date().toISOString()})});return json(res,201,{message:rows[0]})}
 return json(res,405,{error:'Method not allowed'});
}catch(e){json(res,500,{error:e.message})}};
