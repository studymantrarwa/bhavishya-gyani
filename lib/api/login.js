const {req:sbreq,json,makeToken,verifyPassword,cleanEmail}=require('./_lib');
module.exports=async(req,res)=>{try{if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});const b=req.body||{};const identifier=String(b.identifier||b.email||'').trim(),email=cleanEmail(identifier),phone=identifier.replace(/\D/g,''),password=String(b.password||''),type=['user','astrologer','admin'].includes(b.account_type)?b.account_type:'user';const table=type==='user'?'user_accounts':type==='astrologer'?'astrologer_accounts':'admin_accounts';const rows=await sbreq(`/rest/v1/${table}?or=(email.eq.${encodeURIComponent(email)},phone.eq.${encodeURIComponent(phone)})&select=*&limit=1`);const a=rows[0];if(!a||!verifyPassword(password,a.password_salt,a.password_hash))return json(res,401,{error:'Invalid email or password'});if(a.blocked)return json(res,403,{error:'Account blocked'});if(type==='astrologer'&&a.approved!==true)return json(res,403,{error:'Astrologer account is awaiting Admin approval'});const token=makeToken({id:a.id,email:a.email,account_type:type});return json(res,200,{token,user:{id:a.id,email:a.email,name:a.full_name||'',role:type}})}catch(e){return json(res,500,{error:e.message})}};
