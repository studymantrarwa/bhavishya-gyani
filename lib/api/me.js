const {authUser,json,profile}=require('./_lib');
module.exports=async(req,res)=>{try{const u=await authUser(req);if(!u)return json(res,401,{error:'Login required'});const p=await profile(u.id);if(!p)return json(res,401,{error:'Account profile not found'});json(res,200,{user:{id:p.id,email:p.email||u.email,name:p.full_name||'',role:p.role,phone:p.phone||null,avatar_url:p.avatar_url||null,blocked:!!p.blocked}})}catch(e){json(res,500,{error:e.message})}};
