BHAVISHYA GYANI V25 — FRESH INSTALL

1) GitHub: delete the old repository files and upload ALL files from this ZIP.
2) Supabase: run supabase/setup.sql ONCE in SQL Editor. It resets old app data and old Supabase Auth test users.
3) Vercel Environment Variables (Production):
   SUPABASE_URL = your Supabase project URL
   SUPABASE_SERVICE_ROLE_KEY = your Supabase service-role key
   BLOB_READ_WRITE_TOKEN = your Vercel Blob read-write token
   APP_AUTH_SECRET = a long random secret (recommended)
   EPHEMERIS_SECRET = optional secret; leave blank if not using one
4) Redeploy Vercel.
5) Do NOT put service-role keys or passwords in GitHub.
6) Admin registration code starts at 1234 and can be changed in public.system_settings.
7) Passwords are stored only as salted hashes. The database never stores readable passwords.
8) The same email/mobile can be used once in each account type: User + Astrologer + Admin.


V29 CHAT FIX: Replaced invalid PostgREST coalesce() ordering with supported nullslast field ordering plus a created_at fallback. No Supabase SQL change required.
